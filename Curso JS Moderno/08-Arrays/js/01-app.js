// creacion de un Array

// Manera mas utilizada para crear un arreglo
const numeros = [10];

// Forma de crear un arreglo con un constructor
const meses = new Array('Enero', 'Febrero', 'Marzo');

console.log(numeros);
console.log(meses);

// Un arreglo que contiene datos de todo tipo

const deTodo = ['Hola', 10, true, 'si', null, {nombre: "Adrian", trabajo: "Programador"}, [1,2,3] ];

console.log(deTodo);